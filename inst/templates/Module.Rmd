---
title: {{{ data }}}
output: html_document
---

---
requires: {{{ requires }}}
produces: {{{ data }}}.rda
---

```{r Load}
outputDir <- "../gathered/"
```

```{r Save}
save(file = paste0(outputDir, "{{{ data }}}.rda"))
```

