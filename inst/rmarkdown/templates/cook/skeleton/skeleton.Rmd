---
title: {{{ TITLE }}}
params: 
  inputDir: "gathered/"
  thisDir:  "cook/"
  outputDir: "cooked/"
  fileStem: {{{ FILE_STEM }}}
  inputFiles: 
    {{{ INPUT_FILES }}}
---

```{r KnitrOpts, echo=FALSE}
knitr::opts_knit$set(root.dir = normalizePath('../'))
knitr::opts_chunk$set(echo=TRUE, message=TRUE, warning=TRUE, error=TRUE)
```

```{r Load, results='hide'}
loadedObjects <- represtools::LoadInput(params)
```

```{r}
# Cook some data!
```

```{r Save}
lstObjects <- represtools::GatherObjects(pattern = c("df", "plt"), ignore = loadedObjects)
```

```{r}
represtools::DescribeObjects(lstObjects, c(str, summary))
```

```{r }
save(file = paste0(params$outputDir, params$fileStem, ".rda")
     , list = c(lstDfs, lstPlots))
```
